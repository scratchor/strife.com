/*
 * VkLogin Messages
 *
 * This contains all the text for the VkLogin component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.VkLogin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VkLogin component!',
  },
});
