/*
 * GoogleLogin Messages
 *
 * This contains all the text for the GoogleLogin component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GoogleLogin';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GoogleLogin component!',
  },
});
